{
  "name": "kusarium",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "KUSARIUM — クサの植物標本園",
  "scripts": {
    "dev": "npm run build && node scripts/dev-server.mjs",
    "build": "eleventy",
    "check": "node scripts/check-site.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.6"
  }
}
