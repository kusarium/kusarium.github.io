<header class="site-header" data-site-header>
  <div class="site-header__inner shell">
    <a class="brand" href="/" aria-label="{{ site.name }} 首頁">
      <span class="brand__mark{% if branding.headerMark %} brand__mark--custom{% endif %}" aria-hidden="true">
        {% if branding.headerMark %}
          <img src="{{ branding.headerMark }}" alt="">
        {% else %}
          <span>{{ branding.fallbackLetter | default("K") }}</span><i>{{ branding.fallbackNumber | default("26") }}</i>
        {% endif %}
      </span>
      <span class="brand__text">
        <span class="brand__name">{{ site.name }}</span>
        <span class="brand__jp">{{ site.subtitle }}</span>
      </span>
    </a>

    <button class="nav-toggle" type="button" aria-expanded="false" aria-controls="site-navigation" data-menu-toggle>
      <span class="nav-toggle__label">{{ navigation.menuLabel | default("園內導覽") }}</span>
      <span class="nav-toggle__icon" aria-hidden="true"><i></i><i></i></span>
    </button>

    <nav class="site-nav" id="site-navigation" aria-label="{{ navigation.ariaLabel | default('主要導覽') }}" data-menu>
      <ul class="site-nav__list">
        {% for item in navigation.items %}
          {% set active = page.url | isActive(item.url) %}
          <li>
            <a class="site-nav__link{% if active %} is-active{% endif %}" href="{{ item.url }}"{% if active %} aria-current="page"{% endif %}>
              <span>{{ item.label }}</span>
              <small>{{ item.en }}</small>
            </a>
          </li>
        {% endfor %}
      </ul>
    </nav>
  </div>
</header>
