---
layout: layouts/base.njk
title: 採集簿
description: 依標籤瀏覽 KUSARIUM 的圖文記錄。
permalink: /tags/index.html
eleventyComputed:
  title: "{{ pageHeaders.tags.title }}"
  description: "{{ pageHeaders.tags.description }}"
---
<header class="page-intro shell page-intro--split">
  <div>
    <p class="eyebrow">{{ pageHeaders.tags.eyebrow }}</p>
    <h1>{{ pageHeaders.tags.title }}</h1>
  </div>
  <p>{{ pageHeaders.tags.description }}</p>
</header>

<nav class="tag-cloud shell" aria-label="標籤快速導覽">
  {% for topic in collections.topicList %}
    {% set topicPosts = collections.posts | filterByTopic(topic) %}
    <a href="#{{ topic | topicSlug }}"><span>#{{ topic }}</span><small>{{ topicPosts.length }}</small></a>
  {% endfor %}
</nav>

<section class="tag-index shell section">
  {% for topic in collections.topicList %}
    {% set topicPosts = collections.posts | filterByTopic(topic) %}
    <section class="tag-group" id="{{ topic | topicSlug }}">
      <header>
        <h2>#{{ topic }}</h2>
        <p>{{ topicPosts.length }} 枚記錄</p>
      </header>
      <ol>
        {% for post in topicPosts %}
          <li>
            <time datetime="{{ post.date | dateIso }}">{{ post.date | dateReadable }}</time>
            <a href="{{ post.url }}" lang="{{ post.data.language | default(site.language) }}">{{ post.data.title }}</a>
          </li>
        {% endfor %}
      </ol>
    </section>
  {% endfor %}
</section>
