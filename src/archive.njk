---
layout: layouts/base.njk
title: 年月归档
description: 按年份浏览 KUSARIUM 的全部记录。
permalink: /archive/index.html
---
<header class="page-intro shell">
  <p class="eyebrow">CHRONOLOGICAL CABINET</p>
  <h1>年月归档</h1>
  <p>每一次记录都是生长轮上的一条细线。</p>
</header>

<section class="archive shell section">
  {% for group in collections.posts | groupByYear %}
    <div class="archive-year">
      <div class="archive-year__heading">
        <span>{{ group.year }}</span>
        <small>{{ group.posts.length }} RECORDS</small>
      </div>
      <ol>
        {% for post in group.posts %}
          <li>
            <time datetime="{{ post.date | dateIso }}">{{ post.date | dateReadable }}</time>
            <a href="{{ post.url }}">{{ post.data.title }}</a>
            {% if post.data.cover %}<span class="archive-year__image" aria-label="含图片">▧</span>{% endif %}
          </li>
        {% endfor %}
      </ol>
    </div>
  {% endfor %}
</section>
